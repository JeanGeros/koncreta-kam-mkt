---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';

interface Props {
	title: string;
	description: string;
	pubDate: Date;
	categoryLabel: string;
	heroImage?: string | null;
	heroImageAlt?: string | null;
}

const { title, description, pubDate, categoryLabel, heroImage, heroImageAlt } = Astro.props;
---

<html lang="es">
	<head>
		<BaseHead title={`${title} — Koncreta Prefabricados`} description={description} />
		<style>
			.hero-image {
				width: 100%;
				aspect-ratio: 21 / 9;
				overflow: hidden;
				background: var(--negro);
			}
			.hero-image img {
				width: 100%;
				height: 100%;
				object-fit: cover;
			}
			.prose {
				width: 720px;
				max-width: calc(100% - 2.5rem);
				margin: 0 auto;
				padding: 48px 0 90px;
				color: var(--gris-oscuro);
				line-height: 1.75;
			}
			.prose h1,
			.prose h2,
			.prose h3 {
				color: var(--negro);
				margin: 1.6em 0 0.6em;
			}
			.prose p {
				margin: 0 0 1.2em;
			}
			.prose ul,
			.prose ol {
				margin: 0 0 1.2em;
				padding-left: 1.4em;
			}
			.prose li {
				list-style: initial;
				margin-bottom: 0.5em;
			}
			.prose a {
				color: var(--naranja);
				text-decoration: underline;
			}
			.prose img,
			.prose video {
				width: 100%;
				border-radius: 12px;
				margin: 1.6em 0;
			}
			.title {
				max-width: 720px;
				margin: 0 auto;
				padding: 48px 20px 0;
			}
			.title .fb-tag {
				color: var(--naranja);
				font-family: 'Century Gothic', sans-serif;
				font-weight: 700;
				font-size: 0.72rem;
				text-transform: uppercase;
				letter-spacing: 0.08em;
			}
			.title h1 {
				font-size: clamp(1.8rem, 4vw, 2.6rem);
				margin: 14px 0 12px;
				line-height: 1.15;
			}
			.date {
				font-family: 'JetBrains Mono', monospace;
				font-size: 0.78rem;
				color: var(--gris-plata);
			}
		</style>
	</head>

	<body>
		<Header />
		<main>
			<article>
				<div class="title">
					<span class="fb-tag">{categoryLabel}</span>
					<h1>{title}</h1>
					<div class="date"><FormattedDate date={pubDate} /></div>
				</div>
				{
					heroImage && (
						<div class="hero-image" style="margin-top:32px;">
							<img src={heroImage} alt={heroImageAlt ?? ''} />
						</div>
					)
				}
				<div class="prose">
					<slot />
				</div>
			</article>
		</main>
		<Footer />
	</body>
</html>
